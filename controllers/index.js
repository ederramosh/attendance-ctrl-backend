const { submitNewGroups,
        getAllGroups,
        getGroupById,
        updateGroupById,
        removeGroupById, } = require('./Groups.controller');

module.exports = {
    submitNewGroups,
    getAllGroups,
    getGroupById,
    updateGroupById,
    removeGroupById,
}